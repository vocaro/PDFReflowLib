# Final magazine integration qualification

Measured 2026-09-23 on macOS 27 arm64. The frozen normal-basename `pdf-reflow` executable is identified by SHA-256 in `results.json`; source revision, every complete case receipt, EPUB hash, source-page count, and fast-gate timings are recorded there. Raw PDFs, EPUBs, renders, and logs remain outside Git.

This integration closes #191 and #214. Its frozen runtime is `53cb03f4`, with contracts and generated counts at `84de8a14`; the separate closure commit carries this record. USDA source pages 5, 8, 9 and 19 retain complete photo captions; page 16 retains its drop cap, and page 23 emits its running foot once. TechPort pages 2–4 retain sidebars and painted table cells; its five pages keep the large header. Pull quotations are semantic quotations or asides, not navigation headings. USDA articles and index, and TechPort gallery, read complete columns and cards in source order.

USDA page 12 continues the Pest Management Research paragraph across its separate pull quotation; page 17 continues the text beside its photo and credit. Technical discretionary line endings join pyrethroids, neonicotinoids, and assay using source font evidence, while attested hard compounds remain hyphenated. The owner's choice permits strong-font-evidence joins even when both halves are dictionary words; source-attested compounds still veto. The Fed's unquoted chapter-opening summaries are asides outside navigation. Source-framed Fed sidebars can be stepped around only when their page-local membership is complete; all nine reviewed body paragraphs continue across page markers. Two GPO 9/11 report boxes genuinely open the next page before the body continues, so those keep source box order and separate body blocks.

The final source-page contracts include NOAA all 272 complete contents entry/locator pairs, NOAA photo-caption/column and 65→66 continuation checks, FAA caption/body seams, Fed box text and reading order, USDA body columns, and TechPort tables. All 21 complete documents pass 1,363 source checks on 255 reviewed pages spanning 5,267 source pages, with their existing source-identity and memory ceilings, structural checks and EPUBCheck. The fast lane passes all 11 gates, including 854 Swift and 252 Python tests. The isolated FAA structure index peaks at 121,077,760 bytes below 192 MiB; three repeated Fed conversions produce identical EPUBs and add 1,645 leaked objects each below the 4,050-object ceiling. These are Mac measurements.

A prequalification candidate failed DGA's Mac RSS ceiling. Page-wide alpha-mask sample accounting corrected that without changing the limit. A later full candidate exposed four USDA wide-caption column regressions; body-column plans now take precedence. The next run exposed NOAA page 65's ordinary paragraph being misclassified as a closed box; a single-paragraph ownership veto restored its heading/body order and cross-page continuation. One GPO box-order assertion had the wrong capitalization and now matches the printed item. Each failed run remains in external logs and is excluded from passing receipts.

Census OCR showed an environment-sensitive variation during qualification, outside #191/#214. V3 missed OCR text on ten recognized pages and split four genuine cross-page prose paragraphs. An isolated rerun of the exact frozen v2 executable reproduced that same degraded output, so the later code changes are not established as its cause. The final v4 Census output recovers all four paragraph continuations and matches the original successful v2 output exactly in every parsed page, image byte, and warning; source-page reference images were unchanged throughout. The remaining Census fidelity follow-up is #149. A broad all-page comparison also finds OCR transcription differences in CIA and Warren between v3 and v4, though their recognized page images are retained and their target-independent corpus checks pass; no cause is established for those differences. Their existing OCR follow-ups include #209 and #216. Corpus checks are sampled qualification, not a claim that every page is readable as reflowed text or that Mac memory measurements establish an iOS device budget.

Independent final audits of NOAA/FAA, Fed/GPO, and USDA/TechPort/DGA are retained outside Git at `/tmp/pdfreflow-drawn-v4-audit`, `/tmp/pdfreflow-noaa-heading-final-fed-v4-audit`, and `/tmp/pdfreflow-final-usda-techport-audit/v4`. Full gate logs and earlier failed candidates remain in `/tmp/pdfreflow-final-qualified-v[1-4]-*`; the machine-readable passing receipt is `results.json`.
